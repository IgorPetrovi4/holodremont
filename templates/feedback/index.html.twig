{% extends 'admin/index.html.twig'%}


{% block title %}Feedback index{% endblock %}
{% block heading %} Обратная связь {% endblock %}
{% block heading_right %}
  {#  <li> <a class="btn btn-info" href="{{ path('feedback_new') }}" role="button">Создать</a></li>
#}
{% endblock %}
{% block body %}

    <table class="table">
        <thead>
            <tr>
                <th>№</th>
                <th>Клиент</th>
                <th>Контакты</th>
                <th>Информация о заказе</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
        {% for feedback in feedback %}
            <tr>
                <td>{{ feedback.id }}</td>
                <td>{{ feedback.name }}<br>{{ feedback.surname }}</td>
                <td>{{ feedback.phone }}<br>{{ feedback.email }}</td>
                <td>{{ feedback.letter }}</td>
                <td>

                    {{ include('feedback/_delete_form.html.twig') }}
                    {#<a href="{{ path('feedback_show', {'id': feedback.id}) }}">
                        <span style="font-size: 30px; color: Teal;">
                            <i class="fas fa-eye" ></i>
                        </span>
                    </a>

                    <a href="{{ path('feedback_edit', {'id': feedback.id}) }}">
                        <span style="font-size: 30px; color: Teal;">
                            <i class="fas fa-edit"></i>
                        </span>
                    </a>#}
                </td>

            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
