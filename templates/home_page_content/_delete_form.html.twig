<form method="post" action="{{ path('home_page_content_delete', {'id': home_page_content.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ home_page_content.id) }}">
    <button class="btn btn-danger ml-2"><i class="fas fa-trash-alt"></i></button>
</form>
