{% extends 'admin/index.html.twig'%}


{% block title %}Orders index{% endblock %}
{% block heading %} Прайс {% endblock %}
{% block heading_right %}
    <li> <a class="btn btn-info" href="{{ path('orders_new') }}" role="button">Создать</a></li>


{% endblock %}
{% block body %}

    <table class="table">
        <thead>
            <tr>
                <th>№</th>
                <th>Услуга</th>
                <th>Цена</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
        {% for order in orders %}
            <tr>
                <td>{{ order.id }}</td>
                <td>{{ order.service }}</td>
                <td>{{ order.price }}</td>
                <td>
                 {#   <a href="{{ path('orders_show', {'id': order.id}) }}">
                        <span style="font-size: 30px; color: Teal;">
                            <i class="fas fa-eye" ></i>
                        </span>
                    </a>#}

                    <a href="{{ path('orders_edit', {'id': order.id}) }}">
                        <span style="font-size: 30px; color: Teal;">
                            <i class="fas fa-edit"></i>
                        </span>
                    </a>
                </td>

            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
